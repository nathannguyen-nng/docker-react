language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t nathannguyennng/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nathannguyennng/docker-react npm run test
